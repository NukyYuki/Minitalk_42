#include "minitalk.h"

void	sig_handler(int signum, siginfo_t *info, void *extra)
{

}

int	main(void)
{
	ft_printf("Server PID=%d\n", getpid());

	Signal(int, void *, int bool);

	while (1)
	{
		pause();
	}
}
